extends index

block scripts
    script(src="javascripts/login.js")

block ui
    .main.container
        .row
            .col-md-6.col-md-offset-3
                h1.display-4.m-b-2 Please enter your login credentials to continue.

                // registration form
                form(method='POST' action='/login')
                    div.form-group
                        label(for='username') Username:
                        input#username.form-control(type='text', placeholder='' name='username')
                    div.form-group
                        label(for='password') Password:
                        input#password.form-control(type='password' name='password')
                    div.form-group
                        button.btn.btn-primary(id="loginButton" type='submit') Log in

                h3 Not a member?
                    a(href="./register/") Sign up!
    //script
        |$(document).ready(function(){
        |   $("#loginButton").click(function() {
        |       window.location = "login?username=" + $("#username").val() +"&passwordHash=" + createHash($("#password").val());
        |   });
        |});


